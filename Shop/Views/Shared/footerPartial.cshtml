@model Shop.Models.MenuModel
<div class="container">
    <hr>
    <!-- Footer -->
    <footer>
        <div class="row">
            <div class="col-md-4">
                @if (WebSecurity.IsAuthenticated)
                {
                    if (User.IsInRole("Admin"))
                    {
                        <div><a href="@Url.Action("Administrator", "Admin")">@WebSecurity.CurrentUserName</a></div>
                    }
                    using (Html.BeginForm("LogOff", "Account"))
                    {
                        @Html.AntiForgeryToken()
                        <div><input type="submit" class="btn btn-blue" value="Выход" /></div>
                    }
                }
            </div>

            <div class="col-md-4">
                


            </div>
            <div class="col-md-4">
                @{ Html.RenderPartial("AddMailingPartial"); }
            </div>

            

           
        </div>
        <div class="row">
            <div class="col-lg-12 text-center">
                Разработка <a href="mailto:vishnyakovd@gmail.com">Вишняков Дмитрий</a>
            </div>
        </div>
    </footer>
</div>